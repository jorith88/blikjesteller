$jsMinifyTool.toScriptTag("/global/js/fastclick.js")

#if(!$jsMinifyTool.getFiles("page").isEmpty())
    $jsMinifyTool.toScriptTag("page")
#end

<script>
	if ('addEventListener' in document) {
		document.addEventListener('DOMContentLoaded', function() {
			FastClick.attach(document.body);
		}, false);
	}
</script>