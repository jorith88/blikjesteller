node {
    checkout scm

    stage('Build frontend') {
        sh 'npm install'
        sh 'gulp'
    }

    stage('Build Docker image') {

    }

    stage ('Publish Docker image') {

    }

    stage ('Deploy') {

    }
}